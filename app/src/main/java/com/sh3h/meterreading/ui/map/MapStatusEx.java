package com.sh3h.meterreading.ui.map;


public enum MapStatusEx {
    VIEW_MAP, // view the map
    LOCATE_MAP, // view the map with the location
    MARK_MAP, // mark the map
    TRACK_MAP // render the trace on the map
}
